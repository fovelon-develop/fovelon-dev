# Optional. If you don't set it, the app runs in DEMO mode (no OpenAI calls).
OPENAI_API_KEY=

# Optional. If you don't set it, the app uses a local SQLite file: ./app.db
# For Postgres later, example:
# DATABASE_URL=postgresql+psycopg2://user:password@host:5432/dbname
DATABASE_URL=
